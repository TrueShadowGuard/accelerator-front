{
  "name": "accelerator_front",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "cd front && npm run build",
    "install": "cd front && npm install",
    "start": "node index.js",
    "dev": "npm start --prefix front",
    "heroku-postbuild": "npm install --prefix front && npm run build --prefix front"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "events": "^3.3.0",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "prettier": "2.5.1"
  }
}
